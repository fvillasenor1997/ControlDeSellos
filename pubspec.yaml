name: controldesellos
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # Librería principal para manipular PDF
  syncfusion_flutter_pdf: ^26.1.35
  # Para seleccionar el archivo
  file_picker: 
  # Para guardar el archivo en el dispositivo
  path_provider: ^2.0.2
  # Para abrir el archivo guardado y previsualizarlo
  open_file: ^3.3.2
  desktop_drop: ^0.4.4

  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
